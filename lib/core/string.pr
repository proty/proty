extern strcmp(first : #str, second : #str) => #bool

class String < Object
    value : #str

    def init(value : #str)
        @value = value
    end

    def ==(other : String) => Bool
        return Bool::new(strcmp(@value, other.value))
    end
end